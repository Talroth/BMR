function batchProcessDataObject() {
    this.Schema = schema;
}

const schema = new Schema({
    // ID: ObjectId,
    // BatchNumber: String,
    // StartDate: Date,
    // EndDate: Date
   });


return module.exports = batchProcessDataObject;